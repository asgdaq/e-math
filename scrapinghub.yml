# scrapinghub.yml
version: '1.0'

project: 778118  # Replace with your actual project ID

build:
  dockerfile: Dockerfile  # Specify the Dockerfile to build your project

  # Specify your Docker image name (optional but recommended)
  image: myusername/my-spring-boot-app:latest

